import { LatitudeConvertPipe } from './latitude-convert.pipe';

describe('LatitudeConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new LatitudeConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
