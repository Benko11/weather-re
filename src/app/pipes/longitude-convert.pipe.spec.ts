import { LongitudeConvertPipe } from './longitude-convert.pipe';

describe('LongitudeConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new LongitudeConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
